<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:automation="clr-namespace:Avalonia.Automation;assembly=Avalonia.Controls"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="40" d:DesignHeight="40"
             x:Class="SkillChat.Client.Views.Settings.Header">

    <Button
        automation:AutomationProperties.AutomationId="HeaderMoreButton"
        VerticalAlignment="Center"
        Background="Transparent"
        BorderThickness="0"
        Cursor="Hand"
        Command="{Binding ContextMenuCommand}"
        Classes="more">
        <Image Name="more" Width="20" Height="40" />
    </Button>
</UserControl>
